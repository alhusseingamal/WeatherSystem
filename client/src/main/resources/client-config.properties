# Server Configuration

# For local testing
server.base.url=http://localhost:8080/api/weather

# For LAN testing (update with server's actual IP)
# server.base.url=http://192.168.1.7:8080/api/weather

# Connection Settings
connection.timeout.ms=5000
max.retry.attempts=3

# Display Settings
temperature.unit=C